meta {
  name: getActiveUser
  type: http
  seq: 1
}

get {
  url: {{HOST}}/getActiveUser()
  body: none
  auth: inherit
}

docs {
  # getActiveUser() - Function Import 
  
  'getActiveUser' is an API endpoint meant the read the current requisting client's user ID and returning the related data from the system regarding the requester. 
  
  It is intended to be used by client applications to quickly get information about who is currently active in the session, such that they can portray the information visually within the application. 
}
