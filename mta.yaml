_schema-version: 3.3.0
ID: techtransition_mgt
version: 0.9.0
description: "Solution Central"
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  # Service layer
  - name: techtransition_mgt-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: techtransition_mgt-auth
      - name: techtransition_mgt-postgres
      - name: techtransition_mgt-destination
      - name: techtransition_mgt-cloud-logging
      - name: techtransition_mgt-autoscaler

  # Database deployer
  - name: techtransition_mgt-postgres-deployer
    type: nodejs
    path: gen/pg
    parameters:
      buildpack: nodejs_buildpack
      no-route: true
      no-start: true
      tasks:
        - name: deploy-to-postgresql
          command: npm start
    requires:
      - name: techtransition_mgt-postgres

  # Approuter
  - name: techtransition_mgt-router
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: techtransition_mgt-auth
      - name: techtransition_mgt-destination
      - name: techtransition_mgt-cloud-logging
      - name: techtransition_mgt-html5-repo-rt
      - name: techtransition_mgt-autoscaler
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

  # HTML5 App: Solution Central
  - name: techtransition_mgt-solutioncentral
    type: html5
    path: app/solutioncentral
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms: []

  # HTML5 App: Technology Radar
  - name: techtransition_mgt-techradar
    type: html5
    path: app/techradar
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms: []

  # HTML5 App: Config
  - name: techtransition_mgt-config
    type: html5
    path: app/config
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms: []

  # HTML5 Content Deployer
  - name: techtransition_mgt-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: techtransition_mgt-html5-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - name: techtransition_mgt-solutioncentral
          artifacts:
            - com.gavdilabs.techtransmgt.solutioncentral.zip
          target-path: resources/
        - name: techtransition_mgt-techradar
          artifacts:
            - com.gavdilabs.techradar.zip
          target-path: resources/
        - name: techtransition_mgt-config
          artifacts:
            - com.gavdilabs.config.zip
          target-path: resources/

resources:
  - name: techtransition_mgt-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: techtransition_mgt-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: 'View (techtransition_mgt ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.View'
          - name: 'Developer (techtransition_mgt ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Developer'
          - name: 'Reviewer (techtransition_mgt ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Reviewer'
          - name: 'Maintainer (techtransition_mgt ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Maintainer'
          - name: 'Admin (techtransition_mgt ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Admin'
          - name: 'Approver (techtransition_mgt ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Approver'
    requires:
      - name: app-api
  - name: techtransition_mgt-postgres
    type: org.cloudfoundry.managed-service
    parameters:
      service: postgresql-db
      service-plan: development
  - name: techtransition_mgt-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
  - name: techtransition_mgt-cloud-logging
    type: org.cloudfoundry.managed-service
    parameters:
      service: cloud-logging
      service-plan: dev
      config:
        ingest_otlp:
          enabled: true
  - name: techtransition_mgt-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: techtransition_mgt-html5-repo-rt
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
  - name: techtransition_mgt-autoscaler
    type: org.cloudfoundry.managed-service
    parameters:
      service: autoscaler
      service-plan: standard
      config:
        instance_min_count: 1
        instance_max_count: 3
        scaling_rules:
          - metric_type: cpu
            threshold: 75
            operator: ">="
            adjustment: "+1"
            breach_duration_secs: 60
            cool_down_secs: 120
          - metric_type: cpu
            threshold: 30
            operator: "<"
            adjustment: "-1"
            breach_duration_secs: 120
            cool_down_secs: 120
          - metric_type: memoryutil
            threshold: 80
            operator: ">="
            adjustment: "+1"
            breach_duration_secs: 60
            cool_down_secs: 120
          - metric_type: memoryutil
            threshold: 40
            operator: "<"
            adjustment: "-1"
            breach_duration_secs: 120
            cool_down_secs: 120
